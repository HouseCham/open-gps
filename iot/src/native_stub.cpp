// Empty placeholder so PlatformIO's native env has at least one source file
// in src/ to compile and a main() to link. The real test entry point is
// test/test_location_payload.cpp (run via `pio test -e native`) and the
// module under test is lib/location_payload/location_payload.cpp — both
// are discovered automatically. This stub exists only so `pio run -e native`
// has a buildable program; it does nothing useful.
int main(int /*argc*/, char** /*argv*/) { return 0; }