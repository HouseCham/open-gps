---
//-- i18n
import { getTranslation } from '@/i18n';
//-- Types
import type { GetStaticPaths } from 'astro';
import type { Language } from '@/types';
//-- Components
import MainLayout from '@/components/astro/layout/MainLayout.astro';
import { DeviceDetailPage } from '@/components/react/features/devices/detail';

export const getStaticPaths: GetStaticPaths = () => [
    { params: { lan: 'en' } },
    { params: { lan: 'es' } },
];

const locale = Astro.params.lan as Language;
const translation = getTranslation(locale);
const title = `${translation.page.deviceDetail.title} · ${translation.layout.appName}`;
---

<MainLayout
    locale={locale}
    title={title}
    pageLabel={translation.page.deviceDetail.title}
>
    <DeviceDetailPage
        client:load
        locale={locale}
        translations={translation.device}
        dateTranslations={translation.date}
        pageLabel={translation.page.devices.title}
    />
</MainLayout>
