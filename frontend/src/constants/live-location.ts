import type { LiveLocationState } from '@/types/api';

/**
 * @constant EMPTY_LIVE_LOCATION_STATE
 * @description An empty live location state object.
 */
export const EMPTY_LIVE_LOCATION_STATE: LiveLocationState = {
    snapshot: null,
    route: [],
};
