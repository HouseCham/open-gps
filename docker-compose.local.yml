services:
  db:
    build:
      context: ./db
    image: open-gps-db
    container_name: open-gps-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-gps_pass}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - ./.data/postgres:/var/lib/postgresql/data
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}
      interval: 5s
      timeout: 5s
      retries: 10

  migrate:
    build: ./migrate
    image: open-gps-migrate
    container_name: open-gps-migrate
    restart: "no"
    command:
      - "-database"
      - "postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD:-gps_pass}@db:5432/${POSTGRES_DB}?sslmode=disable"
      - up
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-gps_pass}
      POSTGRES_DB: ${POSTGRES_DB}
    depends_on:
      db:
        condition: service_healthy

  api:
    build:
      context: ./backend
      target: runtime
    image: open-gps-api
    container_name: open-gps-api
    restart: unless-stopped
    expose:
      - "8080"
    environment:
      API_PORT: ${API_PORT}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
      DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD:-gps_pass}@db:5432/${POSTGRES_DB}?sslmode=disable
      JWT_JWKS_URL: ${JWT_JWKS_URL:-http://localhost/api/auth/.well-known/jwks.json}
      JWT_ISSUER: ${JWT_ISSUER:-http://localhost}
      JWT_AUDIENCE: ${JWT_AUDIENCE:-open-gps-api}
      SERVER_PORT: 8080
      AUTHULA_BASE_URL: ${AUTHULA_BASE_URL}
      AUTHULA_SECRET: ${AUTHULA_SECRET}
      AUTHULA_DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD:-gps_pass}@db:5432/${POSTGRES_DB}?sslmode=disable
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
    depends_on:
      migrate:
        condition: service_completed_successfully

  frontend:
    build:
      context: ./frontend
      args:
        PUBLIC_API_URL: ${PUBLIC_API_URL}
    image: open-gps-frontend
    container_name: open-gps-frontend
    restart: unless-stopped
    expose:
      - "80"
    environment:
      RESEND_API_KEY: ${RESEND_API_KEY}
      WELCOME_EMAIL_TEMPLATE_EN: ${WELCOME_EMAIL_TEMPLATE_EN}
      WELCOME_EMAIL_TEMPLATE_ES: ${WELCOME_EMAIL_TEMPLATE_ES}
      EMAIL_FROM: ${EMAIL_FROM}
    volumes:
      - frontend_dist:/app/dist
    depends_on:
      - api

  nginx:
    build:
      context: ./nginx
    image: open-gps-nginx
    container_name: open-gps-nginx
    restart: unless-stopped
    ports:
      - "${NGINX_PORT:-443}:443"
      - "${NGINX_HTTP_PORT:-80}:80"
    volumes:
      - frontend_dist:/var/www/html:ro
      - ./nginx/certs:/etc/nginx/certs:ro
      - ./nginx/nginx.local.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - frontend
      - api

volumes:
  frontend_dist: